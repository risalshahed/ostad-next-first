/** @type {import('next').NextConfig} */
const nextConfig = {
  // generateEtags: false,   // user der cache clear krar jnno temporarily eta use kra jaite pare
  distDir: 'build',   // app build korle jei folder create hbe, tar name hbe "build"
}

module.exports = nextConfig
